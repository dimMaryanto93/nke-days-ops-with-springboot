version: '3.9'
services:
  backend:
    image: dimmaryanto93/demo-nke-ops:webservice-v1
    build:
      context: ./
      dockerfile: Dockerfile
      args:
        JAR_FILE: demo-nke-ops-1.0.0.jar
    ports:
      - "8080:8080"
    environment:
      SERVER_PORT: 8080