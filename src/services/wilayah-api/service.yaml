apiVersion: v1
kind: Service
metadata:
  name: wilayah-api
spec:
  selector:
    app: wilayah-api
    tier: backend
    version: v1
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: NodePort